services:
  - type: web
    name: telegram-translator-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn translator:app"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: WEBHOOK_URL
        sync: false
