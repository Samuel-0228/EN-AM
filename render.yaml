services:
  - type: web
    name: translator-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 translator.py
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false
